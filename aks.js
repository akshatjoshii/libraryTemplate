(function (global) {

    var ak$ = function () {
        return new ak$.init();
    };

    ak$.prototype = {

    };

    ak$.init = function () {

    };

    ak$.init.prototype = ak$.prototype;

    global.ak$ = global.aksJs = ak$;

}(window));